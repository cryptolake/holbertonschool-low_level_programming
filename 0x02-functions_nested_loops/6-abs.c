#include "main.h"

/**
 * _abs - calculates the absolute value
 * @n: number to calculate
 *
 * Return: absolute value
 **/
int _abs(int n)
{
	if (n <= 0)
	{
		return -n;
	}
	else
	{
		return n;
	}
}
